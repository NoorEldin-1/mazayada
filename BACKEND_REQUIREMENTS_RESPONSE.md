# رد الباك اند على «مطلوب من الباك — تعديلات العميل (مزايدة)»

ردّ على `BACKEND_REQUIREMENTS.pdf` (15 سبتمبر 2026) و`muzayda_edits.xlsx`.
مرتّب **بنفس ترقيم أقسام الـ PDF**، وكل بند فيه: اللي اتعمل ← شكل الرد ← ملاحظات.

- الشكل الموحّد ماتغيّرش: `{data, message, meta}`، والمبالغ `{amount, formatted}` بالدينار.
- **مافيش أي تغيير كاسر.** كل الحقول الجديدة إضافات، والمسارات القديمة شغالة زي ما هي.
- التوثيق التفاعلي (`/docs` + `openapi.yaml` + Postman) **اتولّد من جديد** وفيه كل المسارات الجديدة.
- مافيش commit ولا push. كل التعديلات في ملفات الـ repo بس.

---

## 0) الخلاصة

| # | التعديل | الباك | الويب/الأدمن | القسم |
|---|---------|-------|--------------|-------|
| 1 | استرجاع البريد المفقود | ✅ | ✅ صفحة للمواطن | 3 |
| 2 | مراجعة طلبات تغيير البريد | ✅ | ✅ شاشة أدمن | 3.4 |
| 3 | تعطيل شراء دفتر الشروط بعد الانتهاء | ✅ `book_sales_closed` | ✅ الزر بيتقفل | 7 |
| 4 | إخفاء شراء الدفتر عن الأدمن | ✅ `staff_not_allowed` + `meta.viewer.is_staff` | ✅ | 7 |
| 5 | ترقيم جلسات المزايدة | ✅ `session.code` | ✅ | 2.1 |
| 6 | إعادة جدولة جلسة | ✅ | ✅ زرار + نافذة في الأدمن | 2.1 |
| 7 | ترقيم الجلسة المعاد جدولتها | ✅ `session.round` / `session_round` | ✅ وسم «الجلسة رقم» | 2.1 · 2.4 |
| 8 | خفض السعر الافتتاحي عند الإعادة | ✅ | ✅ النسبة بتتدخل وقت الإعادة | 2.1 |
| 9 | تطبيق الخفض في كل إعادة | ✅ تراكمي | ✅ | 2.1 |
| 10 | سجل إعادة الجدولة | ✅ `session.history` | ✅ جدول في صفحة المزايدة بالأدمن | 2.1 |
| 11 | خاصية القطاع | ✅ `sector` | ✅ النسبة في الفئة + تجاوز لكل مزايدة | 2.2 |
| 12 | تطبيق نسبة القطاع | ✅ `min_bid` + رفض 422 | ✅ رسالة + تلميح الحد الأدنى | 2.2 |
| 13 | حقوق النشر والعرض | ✅ | ✅ النشر مقفول لحد الدفع | 7 |
| 14 | سعر النشر حسب مساحة العرض | ✅ | ✅ | 7 |
| 15 | سعر النشر حسب الأولوية | ✅ `publication_priority` + ترتيب | ✅ | 2.3 |
| 16 | إدارة باقات/مساحات النشر | ✅ | ✅ شاشة أدمن | 7 |
| 17 | حساب تكلفة النشر | ✅ | ✅ حساب لحظي قبل الدفع | 7 |
| 18 | تغيير الـ Logo | — | ✅ اتطبّق على الويب والوثائق | — |
| 19 | تغيير أيقونة التطبيق | — | ⏸️ موبايل بس: مشروع Flutter مش على الجهاز | — |
| 20 | «كراسة» ← «دفتر» | ✅ | ✅ | 6 |
| 21 | طباعة وصل المشاركة | ✅ `participation_receipt` | ✅ في مكتبة الوثائق | 2.5 · 4 |
| 22 | تفاصيل وصل المشاركة | ✅ | ✅ | 4.2 |
| 23 | وصل نتيجة المزايدة | ✅ `result_document` | ✅ | 2.5 · 4.3 |
| 24 | Premium Subscription | ✅ `/subscription` | ✅ باقات في الأدمن | 5.1 |
| 25 | إدارة الاشتراكات | ✅ | ✅ صفحة للمواطن + شاشة أدمن | 5.1 |
| 26 | تنبيهات المزايدات للـ Premium | ✅ المشتركين الأول | — | 5.3 |
| 27 | اختيار الأنواع المفضلة | ✅ | ✅ في صفحة المواطن | 5.2 |
| 28 | إشعارات حسب نوع المزايدة | ✅ `new_auction_match` | — | 5.3 |
| 29 | Email للـ Premium | ✅ | — | 5.3 |
| 30 | إدارة تفضيلات الإشعارات | ✅ `/preferences/notifications` | ✅ | 5.2 |

> التعديلات 1 و2 (استرجاع البريد) اتنفّذت في جلسة عمل موازية على نفس الـ repo، وملخّصها في القسم 3.
> التعديل 18 (الـ Logo) برضه اتعمل في جلسة موازية على الويب والوثائق.

---

## 2) إضافات على مسارات موجودة

### 2.1 `GET /api/v1/auctions/{id}` — الجلسات وإعادة الجدولة (5 · 6 · 7 · 8 · 9 · 10)

**اللي اتعمل**
- كل مزايدة بقى ليها **رقم جلسة ثابت** `SES-{السنة}-{تسلسل}` بيتولّد تلقائيًا أول ما تتعمل.
  المزايدات القديمة اتدّالها أرقام بترتيب إنشائها.
- **إعادة الجدولة ما بتمسحش الجلسة القديمة.** بتعمل **جلسة جديدة** بنفس الأصل والشروط وموعد جديد،
  فالمزايدات والمشاركين والوثائق القديمة بتفضل سجل ثابت.
- الجلسة الجديدة: `round + 1`، والسعر الافتتاحي = سعر **الجلسة السابقة** × (1 − النسبة)،
  يعني **الخفض بيتراكم** في كل إعادة (1 000 000 ← 900 000 ← 810 000 بنسبة 10%).
  الكفالة بتتحسب من جديد من السعر الجديد.
- شرط الإعادة: الجلسة **انتهت من غير ترسية** (مفيش عروض) أو **اتلغت**، ولازم تكون **آخر جلسة** في السلسلة.
- النسبة الافتراضية 10% (إعداد `auctions.reschedule_reduction_percent`)، والأدمن يقدر يغيّرها وقت كل إعادة (0–90%).
- لو الجلسة القديمة كان ليها دفتر شروط، بيتولّد دفتر جديد للجلسة الجديدة تلقائيًا، والصور بتتنسخ.

**الرد** — المفتاح `session` **دايمًا موجود**، حتى للجلسة الأولى (`round: 1` و`history: []`):

```jsonc
"session": {
  "round": 2,                       // int
  "code": "SES-2026-0142",
  "start_time": "2026-10-01T09:00:00+01:00",
  "end_time": "2026-10-08T09:00:00+01:00",
  "opening_price": { "amount": 900000, "formatted": "900 000 دج" },   // بعد الخفض
  "reduction_percent": 10,          // 0 في الجلسة الأولى
  "reschedule_count": 1,
  "original_opening_price": { "amount": 1000000, "formatted": "1 000 000 دج" },
  "history": [                      // الأحدث الأول
    {
      "id": "9c1e…",                // إضافة: رقم الجلسة القديمة لو التطبيق عايز يفتحها
      "round": 1,
      "code": "SES-2026-0131",
      "start_time": "2026-09-01T09:00:00+01:00",
      "end_time": "2026-09-08T09:00:00+01:00",
      "opening_price": { "amount": 1000000, "formatted": "1 000 000 دج" },
      "reduction_percent": 0,
      "status": "CLOSED",
      "result_label": "لم تُقدَّم عروض"      // مترجَم من السيرفر
    }
  ]
}
```

- `opening_price` في الجذر = سعر الجلسة الحالية بعد الخفض. ✅
- `result_label` بيرجع واحد من: «تمت الترسية» / «لم تُقدَّم عروض» / «أُلغيت الجلسة»، بلغة `Accept-Language`.
- التواريخ بتوقيت المنصة (`+01:00`) بصيغة ISO-8601، زي باقي الـ API.

### 2.2 القطاع والحد الأدنى للمزايدة (11 · 12)

**اللي اتعمل**
- «القطاع» = **الفئة** (`category`). كل فئة بقى ليها `min_increment_percent` (الافتراضي 0 = القاعدة القديمة).
- الأدمن يقدر يحدّد نسبة **خاصة بمزايدة معيّنة** وقت إنشائها. لو سابها فاضية، بتتطبق نسبة القطاع.
- `min_bid` = السعر الحالي + النسبة، **مقرّب لأعلى لأقرب دينار**، وأقل زيادة دينار واحد.

**أ) على `GET /auctions/{id}`:**
```jsonc
"sector": { "id": "3", "name": "عقارات", "min_increment_percent": 5 },
"min_bid": { "amount": 1050000, "formatted": "1 050 000 دج" },
"min_increment_percent": 5          // إضافة على مستوى الجذر كمان
```

**ب) على `GET /auctions/{id}/price`:**
```jsonc
"min_bid": { "amount": 1050000, "formatted": "1 050 000 دج" },
"min_increment_percent": 5
```

**الفرض في الباك** — `POST /auctions/{id}/bid` تحت الحد بيرجع **422** والخطأ تحت `errors.amount`:
```json
{ "message": "…", "errors": { "amount": ["نسبة الزيادة الدنيا في هذا القطاع 5% من السعر الحالي — أقل مزايدة مقبولة هي 1 050 000 دج."] } }
```
- `min_bid` كائن `{amount, formatted}` مش رقم خام. ✅
- `sector` بيرجع مع `category` الموجود، فمفيش حاجة اتشالت.
- صفحة المزايدة في الويب بتعرض نفس الحد الأدنى وبتحدّثه لحظيًا.

### 2.3 أولوية النشر (15)

```jsonc
"publication_priority": "PRIORITY"   // NORMAL | PRIORITY — دايمًا واحدة من القيمتين
```
- على `AuctionResource` و`AuctionListResource`.
- **الترتيب في الباك:** مزايدات `PRIORITY` بتظهر الأول في `GET /auctions` (وفي صفحة التصفّح على الويب)،
  وبعدها الترتيب اللي المستخدم اختاره.

### 2.4 إضافات `AuctionListResource` (7 · 15)

```jsonc
"session_round": 2,                  // int
"session_code": "SES-2026-0142",     // إضافة
"publication_priority": "PRIORITY"
```
بيرجعوا من `GET /auctions` و`/auctions/search` و`/my-auctions` و`won_auctions` في `/dashboard`.

### 2.5 وصولات المزايدة على `AuctionResource` (21 · 22 · 23)

```jsonc
"participation_receipt": { "id": "9d2f…", "title": "وصل المشاركة — مزايدة رقم SES-2026-0142", "download_url": "https://…/api/v1/documents/9d2f…/download" },
"result_document":       { "id": "7a1c…", "title": "وصل نتيجة المزايدة رقم SES-2026-0142",   "download_url": "https://…/api/v1/documents/7a1c…/download" }
```
- **المفتاح بيغيب** لحد ما الوثيقة تتولّد **للمستخدم ده بالذات** (لازم التوكن).
- التحميل من المسار الموجود `GET /documents/{id}/download` بتوكن Sanctum. **مفيش مسار جديد.**
- رقم المزايدة في العنوان هو رقم الجلسة `session.code`.
- إضافة: `"book_purchase_open": true|false` (تعديل 3، تفاصيله في القسم 7).

---

## 3) استرجاع البريد الإلكتروني المفقود (1 · 2)

> اتنفّذ في جلسة عمل موازية، ودي الملاحظات النهائية منها.

**3.1 `POST /api/v1/auth/email-recovery`** (من غير توكن، `multipart/form-data`)

| الحقل | القاعدة |
|-------|---------|
| `nin` | مطلوب، 18 رقم، لازم يكون موجود |
| `birth_date` | مطلوب، `Y-m-d`، لازم يطابق الحساب |
| `phone` | مطلوب، 10 أرقام تبدأ بـ 0، لازم يطابق الحساب |
| `new_email` | مطلوب، بريد صحيح، مش مستخدم في حساب تاني |
| `selfie_with_id` | مطلوب، jpg/png، ≤ 1024KB |

- الرد **201**: `data {id, status, status_label, new_email_masked, submitted_at, reviewed_at, rejection_reason}`.
- الأخطاء **422** تحت اسم كل حقل. تحت `nin` بيرجع: الرقم غير موجود / فيه طلب قيد المراجعة / حساب موظف.
- **Throttle:** 3 محاولات/ساعة لكل NIN + 10/ساعة لكل IP، وبعدها **429**.

**3.2 `POST /api/v1/auth/email-recovery/status`** — الجسم `{"nin": "…"}`
← آخر طلب بنفس شكل 3.1. **مفيش طلب ← `{"data": null}` بـ 200، مش 404.**

**3.3 الحالات:** `PENDING → UNDER_REVIEW → APPROVED | REJECTED`
- **APPROVED:** البريد بيتغيّر فعلًا، كل التوكنات والجلسات بتتلغي، وبيتبعت بريد + إشعار داخلي + Push (`event: email_recovery_approved`).
- **REJECTED:** `rejection_reason` بيتملا، وبيتبعت إشعار داخلي + Push (`email_recovery_rejected`) وبريد على العنوان الجديد اللي اتطلب.

**3.4 شاشة الأدمن:** `/admin/email-recovery` بتبويبات حسب الحالة وعدّاد في القائمة الجانبية.
صفحة الطلب فيها: مقارنة بيانات الطلب مع الحساب، صورة السيلفي جنب صور التحقق (KYC) القديمة، وأزرار «بدء المراجعة / قبول / رفض مع السبب».
الصلاحيات: نفس صلاحيات مراجعة الـ KYC.
وعلى الويب: صفحة للمواطن `/recover-email`، ورابط «فقدت بريدك الإلكتروني؟» في صفحة الدخول بيوديها.

---

## 4) وصولات المشاركة والنتيجة (21 · 22 · 23)

### 4.1 أنواع وثائق جديدة على `DocumentType`
```php
case PARTICIPATION_RECEIPT = 'PARTICIPATION_RECEIPT'; // وصل المشاركة
case AUCTION_RESULT        = 'AUCTION_RESULT';        // وصل نتيجة المزايدة
```
- بيظهروا في `GET /documents` ضمن مكتبة المستخدم. ✅
- مقبولين كفلتر `type[]` في `GET /documents`، وموجودين في `types` في `GET /documents/filters`. ✅
- مترجمين في `lang/*/enums.php`، فـ `type_label` بيرجع صح بالتلات لغات. ✅
- `GET /documents/summary`: عدّاد `receipts` بقى بيشملهم، ومعاه حقلين جداد `participation_receipts` و`results`.

### 4.2 توليد وصل المشاركة (21 · 22)
بيتولّد **مرة واحدة** لحظة تأكيد دفع الكفالة، ولو نفس رسالة الدفع وصلت تاني ما بيتكررش. المحتوى:
- **المزايدة:** الرقم/المرجع، العنوان، الجهة المنظّمة، الولاية، رقم الجلسة، تاريخ البداية والنهاية، السعر الافتتاحي.
- **المواطن:** الاسم، رقم التعريف الوطني مخفي جزئيًا، الهاتف.
- **المشاركة:** تاريخ التسجيل، مبلغ الكفالة المدفوع، ثمن دفتر الشروط، مرجع عملية الدفع (`gateway_ref`)، رقم المشارك (ترتيب تسجيله في المزايدة).
- **توقيع إلكتروني + QR** بنفس آلية دفتر الشروط، و`verify_url` شغال.

> ملاحظة: الإكسيل قال «بعد شراء دفتر الشروط». التزمنا بالـ PDF (بعد تأكيد التسجيل) لأن الوصل
> بيحتوي على مبلغ الكفالة ورقم المشارك، ودول مش موجودين غير بعد التسجيل.

### 4.3 وصل النتيجة (23)
بيتولّد عند إقفال المزايدة **لكل مشارك دفع الكفالة**، مش للفايز بس (الفايز بياخد برضه وثيقة الترسية الموجودة).
المحتوى: تفاصيل المزايدة ورقم الجلسة، النتيجة (تمت الترسية / لم تقدّم عروض)، الفايز بالاسم المستعار (alias)، سعر الرسو،
عدد المزايدات وعدد المزايدين، و**وضعية المواطن نفسه**: أعلى عرض قدّمه + ترتيبه (مثلًا 2 / 5) + فايز ولا لأ.
الأرقام دي متجمّدة وقت الإقفال وموقّعة، يعني ما بتتغيرش بعد كده.

---

## 5) Premium والتفضيلات (24 – 30)

### 5.1 `/api/v1/subscription` (24 · 25) — كله بتوكن

**`GET /api/v1/subscription`** — الاشتراك الحالي + الباقات في نداء واحد:
```jsonc
{
  "data": {
    "is_premium": true,
    "subscription": {                    // null لو ماشتركش قبل كده
      "id": "5c1d…",
      "status": "ACTIVE",                // ACTIVE | PENDING | EXPIRED | CANCELLED
      "status_label": "فعّال",
      "plan": {
        "code": "YEARLY",
        "name": "الباقة السنوية",
        "description": "…",
        "period": "YEARLY",              // MONTHLY | YEARLY
        "period_label": "سنوي",           // إضافة
        "price": { "amount": 12000, "formatted": "12 000 دج" },
        "features": ["تنبيه فوري بالمزايدات الجديدة…", "إشعارات البريد الإلكتروني…"],
        "is_recommended": true
      },
      "started_at": "2026-01-01T00:00:00+01:00",
      "expires_at": "2027-01-01T00:00:00+01:00",
      "auto_renew": true,
      "days_remaining": 108              // محسوب في السيرفر
    },
    "plans": [ /* نفس شكل plan */ ]
  }
}
```

**`POST /api/v1/subscription`** — الجسم `{"plan_code": "YEARLY"}` ← نفس شكل باقي المدفوعات بالظبط:
```json
{ "data": { "redirect_url": "https://…", "ref": "PAY-…" } }
```
- الدفعة بتتسجّل في جدول `payments` بنوع **`PaymentType::SUBSCRIPTION`** (مترجم)، وبتظهر في `GET /payments/{ref}/status`. ✅
- رفض **422** بـ `code`: `plan_unavailable` أو `staff_not_allowed`.
- لو المستخدم مشترك أصلًا واشترى تاني، **المدة الجديدة بتتضاف على اللي فاضل**، فمفيش يوم مدفوع بيضيع.

**`DELETE /api/v1/subscription`** — بيوقف التجديد التلقائي بس (`auto_renew = false`)، والمدة المدفوعة شغالة لحد `expires_at`.
بيرجع **نفس شكل الـ GET** بعد التعديل.

**أثر على `UserResource`:** `is_premium` بيتحدّث **فورًا** مع تأكيد الدفع، لأنه بيتقرا من `premium_until` اللي بيتمد لحظة التأكيد. ✅

> **التجديد التلقائي:** مفيش بطاقة بتتحفظ، فالسيرفر مايقدرش يخصم لوحده.
> عند الانتهاء، الاشتراك اللي عليه تجديد تلقائي بيبقى `EXPIRED` وبيتبعت للمستخدم إشعار يجدّد بضغطة.
> لو التجديد كان موقوف، بيبقى `CANCELLED`. قبل الانتهاء بـ 7 أيام (قابلة للتعديل) بيتبعت تذكير.

### 5.2 `/api/v1/preferences/notifications` (27 · 30) — بتوكن

**`GET`:**
```jsonc
{
  "data": {
    "channels": { "push": true, "email": false, "sms": false },
    "auction_categories": [1, 5],        // أرقام؛ فاضية = الكل
    "new_auction_alerts": true,
    "available_categories": [ { "id": 1, "name": "مركبات" }, { "id": 5, "name": "عقارات" } ],
    "email_requires_premium": true,
    "sms_available": false,              // إضافة: مفيش مزوّد SMS حاليًا
    "is_premium": false
  }
}
```
- الإعدادات الافتراضية لمستخدم ما حفظش حاجة: push مفعّل، البريد والرسائل مقفولين، التنبيهات مفعّلة، وكل الأنواع.

**`PUT`** — بياخد **الصورة الكاملة**:
```json
{ "channels": { "push": true, "email": true, "sms": false }, "auction_categories": [1, 5], "new_auction_alerts": true }
```
بيرجّع **نفس شكل الـ GET بعد الحفظ**، والقيم المرفوضة بترجع مصحّحة:
- `email: true` لمستخدم مش مشترك ← بيتحفظ `false` (طالما `email_requires_premium = true`).
- `sms: true` ← بيتحفظ `false` لحد ما يتربط مزوّد SMS.
- أرقام أنواع مش موجودة بتتشال.

### 5.3 إرسال الإشعارات (26 · 28 · 29)

عند **نشر** مزايدة (من الأدمن، أو جلسة معاد جدولتها اتنشرت):
1. المستهدفين: المواطنين اللي `new_auction_alerts = true`، وقائمة أنواعهم فاضية أو فيها نوع المزايدة. الموظفين والمحظورين مستبعدين.
2. القنوات: **إشعار داخل التطبيق دايمًا**، + Push لو مفعّل، + بريد لو مفعّل.
3. **الأولوية للمشتركين (26):** المشتركين بيوصلهم التنبيه **لحظة النشر**، والباقي بعد **30 دقيقة** (إعداد قابل للتعديل).
4. **البريد للمشتركين بس (29)** لما `email_requires_premium = true`.
- كل موجة ما بتتبعتش غير مرة واحدة، حتى لو الجدولة اشتغلت أكتر من مرة.
- المزايدات المنشورة قبل التحديث ده **مش هيتبعتلها تنبيهات**، عشان مانغرّقش المستخدمين بالقديم.

**أحداث جديدة** (نفس القيمة في `NotificationResource.type` وفي `data.type` جوّه الـ Push):

| `event` | الاستخدام | `action_url` |
|---------|-----------|--------------|
| `new_auction_match` | مزايدة جديدة من نوع مفضّل | صفحة المزايدة |
| `subscription_activated` | تأكيد تفعيل الاشتراك | `…/dashboard/subscription` |
| `subscription_expiring` | تنبيه قرب الانتهاء | `…/dashboard/subscription` |
| `subscription_expired` | انتهى الاشتراك | `…/dashboard/subscription` |

`action_url` لإشعارات الاشتراك فيه `/subscription` زي ما اتطلب، فالتطبيق يقدر يستخدمه كاحتياطي. ✅

---

## 6) مصطلح «دفتر الشروط» (20)

- اتغيّر **كل** «كراسة الشروط / كراس الشروط / الكراسة / كراسات» ← «دفتر الشروط / الدفتر / دفاتر»، في:
  `lang/ar/{auctions, mail, enums, payments, documents, admin, auction_reports, notifications, pages, legal}.php`،
  وفي شاشات إنشاء وتعديل المزايدة، وقالب دفتر الشروط، والتعليقات، والتوثيق.
- **صيغة الجملة اتظبطت** («كراسة» مؤنث و«دفتر» مذكّر): مثلًا «دفتر الشروط متاح مجاناً»، «نُشر دفتر الشروط»،
  «غير قابل للاسترداد»، «وأوافق عليه».
- الأهم للتطبيق:
  - `lang/ar/enums.php` ← `'BOOK_PURCHASE' => 'دفتر شروط'` ✅
  - `DocumentType::CONDITION_BOOK` ← `'دفتر الشروط'` ✅
  - `payments.already_bought_book` و`payments.book_free` ✅
- الـ `code` في الأخطاء ماتغيّرش، فالتطبيق مش هيتأثر في أي اتجاه.

---

## 7) ملاحظات تنفيذية

### دور المستخدم في `meta.viewer` ✅
```jsonc
"meta": { "viewer": { "is_staff": false, "role": "CITIZEN", "is_premium": false, "can_bid": true, … } }
```
التطبيق يقدر يقرا `is_staff` مباشرة بدل ما يعتمد على كاش `/profile`.

### تعديل 3 — تعطيل شراء الدفتر بعد الانتهاء ✅
- **الباك:** `POST /auctions/{id}/buy-book` بعد انتهاء وقت المزايدة (أو لو اتقفلت/اتلغت) بيرجع **422** و`"code": "book_sales_closed"`.
- **حقل جديد:** `book_purchase_open` على `AuctionResource`.
- **الويب:** زر الشراء بيتحوّل لزر مقفول «انتهت فترة شراء دفتر الشروط».

### تعديل 4 — إخفاء الشراء عن الأدمن (جانب الويب) ✅
- حسابات الإدارة والجهات **مابتشوفش** زر شراء الدفتر ولا التسجيل في صفحة المزايدة، وبدالهم رسالة توضيحية.
- **وفي الباك كمان:** لو موظف حاول يشتري أو يسجّل، بيرجع **422** و`"code": "staff_not_allowed"`.

### تعديلات 13 – 17 — حقوق النشر والعرض (ويب + باك) ✅
- **باقات النشر (16):** شاشة جديدة `/admin/publication-packages`، والأدمن بيحدّد لكل باقة:
  **مساحة العرض** (القائمة العامة / أعلى قسم القطاع / الصفحة الرئيسية)، سعر النشر العادي، رسوم الأولوية، ومدة العرض.
- **حساب التكلفة (14 · 15 · 17):** في فورم إنشاء المزايدة بيختار الباقة ومستوى النشر (عادي/بأولوية)،
  والتكلفة بتظهر **لحظيًا قبل الدفع** = سعر المساحة + رسوم الأولوية. السيرفر بيعيد حسابها وبيحفظها على المزايدة.
- **الدفع قبل النشر (13):** الجهة المنظّمة بتدفع للمنصة **خارج النظام** (تحويل/وصل)، والأدمن بيسجّل الدفع برقم المرجع
  من صفحة المزايدة. **زر «نشر» مقفول لحد ما الرسوم تتسجّل كمدفوعة.**
- اتعمل كده لأن حسابات الجهات «للقراءة فقط» بقاعدة المنصة، فمش هي اللي بتنشر ولا بتدفع إلكترونيًا.
- الشرط ده يتقفل أو يتفتح من الإعدادات (`publication.require_payment_before_publish`).
- لو مفيش ولا باقة مفعّلة، اختيار الباقة بيبقى اختياري والنشر مجاني، وده سلوك المنصة القديم.
- **الموبايل:** بيستهلك النتيجة بس عن طريق `publication_priority` (القسم 2.3).

### جديد على الويب/الأدمن كمان
- **صفحة للمواطن** `/dashboard/subscription` («العضوية المميزة» في القائمة): الاشتراك الحالي، الباقات،
  وإيقاف التجديد التلقائي، وتفضيلات الإشعارات. بتستخدم نفس خدمات الـ API بالظبط.
- **الأدمن:** `/admin/subscription-plans` لإدارة الباقات (السعر، المدة، المزايا بالتلات لغات، «موصى بها»)،
  و`/admin/subscriptions` لعرض كل الاشتراكات بفلاتر وإحصائيات.
- **الفئات:** حقل «نسبة الزيادة الدنيا للقطاع».
- **صفحة المزايدة في الأدمن:** كارت «الجلسات وإعادة الجدولة» (بالسجل وزر الإعادة)، وكارت «رسوم حقوق النشر».
- **صفحة المزايدة العامة:** وسم «الجلسة رقم ٢ · SES-…»، ووسم «مميّزة» للمزايدات ذات الأولوية، وملاحظة الخفض.
- **الإعدادات:** قسمين جداد («المزايدات والجلسات»، «حقوق النشر والعرض»، «العضوية المميزة والتنبيهات»).

---

## 8) النزول على السيرفر

**ترتيب النزول:** كل البنود ممكن تنزل **مع بعض**، لأن كلها إضافات. لكن لو هتنزل على مراحل، ترتيب الـ PDF شغال زي ما هو.

1. `php artisan migrate --force` — أربع migrations جديدة:
   - `2026_09_15_000001` نسبة القطاع (الفئات + المزايدات)
   - `2026_09_15_000002` الجلسات + حقوق النشر + `publication_packages` (وبيدّي أرقام جلسات للمزايدات الموجودة)
   - `2026_09_15_000003` `subscription_plans` + `subscriptions` + `notification_preferences`
   - `2026_09_15_100001` `email_recovery_requests`
2. الـ seeders (كلها idempotent، ومابتمسحش أي تعديل عمله الأدمن). **اتضافت لسكربتات النزول:**
   - `RolesPermissionsSeeder` ← صلاحيتين جداد: `publication.manage`, `subscriptions.manage`
   - `SystemSettingsSeeder` ← الإعدادات الجديدة
   - `PremiumAndPublicationSeeder` ← باقتين Premium (شهرية 1 500 دج / سنوية 12 000 دج) و3 باقات نشر مبدئية. **الأسعار دي تتراجع من الأدمن قبل الإطلاق.**
3. **الـ Scheduler** (`routes/console.php`) — لازم الـ cron يكون شغال:
   - `auctions:dispatch-alerts` كل دقيقة (موجات التنبيهات)
   - `subscriptions:sweep` كل ساعة (انتهاء الاشتراكات + التذكير)
4. الإعدادات الجديدة (قابلة للتعديل من `/admin/settings`):

| المفتاح | الافتراضي |
|---------|-----------|
| `auctions.reschedule_reduction_percent` | 10 |
| `publication.require_payment_before_publish` | 1 |
| `subscriptions.non_premium_alert_delay_minutes` | 30 |
| `subscriptions.email_requires_premium` | 1 |
| `subscriptions.expiry_reminder_days` | 7 |
| `notifications.sms_enabled` | 0 |

5. ملفات الواجهة اتبنت (`npm run build`) عشان تلميح الحد الأدنى للمزايدة في صفحة المزايدة.

---

## 9) فحص سريع بعد النزول (القسم 7 في الـ PDF) — متغطّي بتستات

- `min_bid` كائن `{amount, formatted}`. ✅
- `session.round` و`session_round` أرقام صحيحة (int). ✅
- `auction_categories` مصفوفة أرقام. ✅
- `publication_priority` يا `NORMAL` يا `PRIORITY` بس. ✅

**التستات:** اتضاف **35 تست** جديد في 3 ملفات:
- `tests/Feature/ClientEditsTest.php` (16): القطاع، الجلسات والخفض التراكمي، الأولوية، رسوم النشر وقفل النشر، شروط شراء الدفتر، الوصولات، الاشتراك والانتهاء، موجات التنبيهات.
- `tests/Feature/Api/V1/ClientEditsApiTest.php` (7): شكل وأنواع كل الحقول الجديدة، 422 تحت `errors.amount`، مسارات الاشتراك والتفضيلات، `meta.viewer`.
- `tests/Feature/ClientEditsPagesTest.php` (5): عرض كل شاشات الويب والأدمن الجديدة والمعدّلة، وحفظ الفورم.

وفوقهم تستات استرجاع البريد من الجلسة الموازية. **نتيجة التشغيل الكامل في آخر الملف.**

---

## 10) قرارات محتاجة تأكيد منكم

1. **رسوم النشر بتتدفع خارج النظام** والأدمن بيسجّلها. لو مطلوب دفع إلكتروني من حساب الجهة، ده محتاج نغيّر قاعدة «الجهة للقراءة فقط».
2. **وصل المشاركة** بيطلع بعد تأكيد الكفالة، مش بعد شراء الدفتر (السبب في 4.2).
3. **التجديد التلقائي** تذكير + تجديد بضغطة، مش خصم تلقائي (مفيش بطاقات محفوظة).
4. **الرسائل النصية (SMS)** مقفولة لحد ما يتحدد مزوّد.
5. **الأسعار المبدئية** للباقات (Premium والنشر) مجرد أمثلة.
6. **أيقونة التطبيق (19)** محتاجة ملف الأيقونة ومشروع Flutter.

---

## نتيجة التشغيل الكامل

```
php artisan test
Tests: 415 passed (1608 assertions)   — قبل التعديلات: 368 passed
```
كل التستات القديمة شغالة من غير تعديل (تحديث وحيد: كلمة «كراسة» ← «دفتر» في بيانات تست)،
ومعاها تست التطابق بين اللغات التلاتة `LocalizationTest`.
الـ migrations الأربعة اتطبّقت على قاعدة MySQL المحلية بنجاح.
